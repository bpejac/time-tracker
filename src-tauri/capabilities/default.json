{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the time tracker tray app",
  "windows": ["history"],
  "permissions": [
    "core:default"
  ]
}
